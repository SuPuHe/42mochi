version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: dockerfiles/Dockerfile.python
    container_name: fastapi_app
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app
    restart: always
